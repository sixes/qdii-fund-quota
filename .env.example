# PostgreSQL Database (Coolify)
# Format: postgresql://username:password@host:port/database
DATABASE_URL="postgresql://postgres:password@localhost:5432/etf_data"

# Optional: Python backend script database connection
# (Defaults to DATABASE_URL values if not set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=etf_data
DB_USER=postgres
DB_PASSWORD=password

# Optional: AWS credentials (only needed for index-constituents API)
# You can remove these after migrating index-constituents to PostgreSQL
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
